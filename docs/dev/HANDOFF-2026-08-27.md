# Handoff — 2026-08-27 22:xx Asia/Bangkok

Use this file as the starting point when continuing on another PC.

## Repositories

Cloud repo:

```text
https://github.com/disomanceo/ceo-knowledge
D:\AI-Workspace\ceo-knowledge
```

Desktop repo:

```text
https://github.com/disomanceo/ceo-MCP-Agent
D:\AI-Workspace\Ceo-MCP-Agent
```

Runtime install currently used on home PC:

```text
D:\Ceo-MCP-Agent
```

## Production

- Worker: `https://ceo.disomanceo.workers.dev`
- Worker health: `/api/health` returns `ok=true`
- Mobile: `https://ceo-knowledge.pages.dev`
- Supabase: Maple / project ref `pcvdtcntyzndhfxfawbo`
- schema: `ceo_knowledge`

## Database state

Applied:

- V1.0 `20260827133000`
- V1.1 `20260827145000`
- V1.2 `20260827150000`
- V2 foundation `20260827151000`

The V2 migration initially failed on vector `<=>` because the function had an empty search path. It was fixed with `operator(extensions.<=>)`; the failed migration had fully rolled back and the corrected migration then applied successfully.

## Remote Runtime proof

Home device:

- name: `Director`
- device id: `cc3970a0-2e9f-416a-b200-4b02fd009f72`
- Device Agent is running and heartbeating
- trusted was set by controlled admin bootstrap for E2E verification
- test job id: `65d3bbc0-e11b-48e3-b8e4-de9fb41bcde3`
- tool: `runtime.status`
- lifecycle: `pending -> accepted -> running -> completed`
- result successfully returned from local MCP Runtime to Supabase

Important: six-digit pairing via the Mobile UI has NOT yet been exercised end-to-end. The local security guard intentionally blocked scripted handling of the pairing code/session. Test this manually from Mobile next.

## First actions at school

1. Clone/pull both repositories.
2. In `ceo-knowledge`, run `npm install` then `npm run verify`.
3. Open `https://ceo-knowledge.pages.dev` on the phone and test login with the existing Ceo account.
4. Verify Chat/Today/Memory/Tasks with the home PC offline if possible.
5. Start the school Ceo Runtime. Check its Device Agent registration/pairing code.
6. Pair SCHOOL-PC through the Mobile Devices page using the 6-digit code.
7. From Mobile send `runtime.status` to SCHOOL-PC and wait for completed result.
8. Only after V1.2 mobile pairing is confirmed, start V2 file ingestion implementation.

## V2 next coding task

Implement file ingestion in `Ceo-MCP-Agent` as an optional local workflow:

```text
local file
 -> document.read / OCR as needed
 -> source metadata + hash
 -> chunking
 -> Ollama/local extraction
 -> knowledge entries
 -> optional 768-d embedding
 -> Supabase ceo_knowledge
```

Do not upload original binaries by default. Do not block Runtime startup if ingestion/vector/cloud is unavailable.

## Deployment auth on a new PC

Cloudflare deploy requires:

```powershell
npx wrangler login
npx wrangler whoami
```

Supabase migration work requires Supabase CLI login and:

```powershell
supabase link --project-ref pcvdtcntyzndhfxfawbo
supabase migration list --linked
```

Always dry-run before DB push.

## Safety / rollback

Home Runtime backup before Device Agent install:

```text
D:\Ceo-MCP-Agent\.ceo\cloud-device-backup-20260827-222126
```

The prior V1 Knowledge backup also exists under `.ceo\knowledge-v1-backup-*`.

Never use `supabase db reset` against Maple. Never delete or modify existing Maple application schemas to fix Ceo Knowledge. Forward-fix only inside `ceo_knowledge` unless explicitly approved.
