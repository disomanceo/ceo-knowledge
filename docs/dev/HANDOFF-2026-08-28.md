# Handoff — 2026-08-28 — SCHOOL-PC

Use this as the current continuation checkpoint. The 2026-08-27 handoff remains as historical context.

## Repositories

- Cloud/Knowledge: `D:\AI-Workspace\ceo-knowledge` — https://github.com/disomanceo/ceo-knowledge
- Desktop Runtime: `D:\AI-Workspace\Ceo-MCP-Agent` — https://github.com/disomanceo/ceo-MCP-Agent
- SCHOOL-PC runtime install: `D:\Ceo-MCP-Agent-ADMIN`

## Production

- Worker: `https://ceo.disomanceo.workers.dev`
- Mobile PWA: `https://ceo-knowledge.pages.dev`
- Supabase Maple: project ref `pcvdtcntyzndhfxfawbo`
- isolated schema: `ceo_knowledge`

## SCHOOL-PC / V1.2

- Device Agent starts and reaches Cloud successfully.
- Runtime/Launcher recovery was exercised while installing V2 and returned to RUNNING.
- Six-digit Mobile pairing remains a UI/UX validation item; do not bypass the pairing security model for production use.

## V2 File Ingestion — E2E PROVEN

Completed on SCHOOL-PC:

```text
Active Project local file
 -> document.verify/read
 -> source metadata + sha256
 -> Ollama extraction
 -> Knowledge entry
 -> bounded chunks
 -> 768-d local embedding
 -> Supabase ceo_knowledge
 -> semantic retrieval
```

Proof points:

- `knowledge.ingest_file` completed against `D:\Ceo-MCP-Agent-ADMIN\docs\CEO-KNOWLEDGE.md`.
- Source dedup returned the same source id for repeated ingestion.
- File-backed Knowledge dedup bug was fixed: source + content fingerprint no longer depends on AI/caller title.
- Repeated ingestion with two different titles returned the same canonical Knowledge id `24685cf0-dcbb-4559-b1d4-e7b97e875973`.
- Local embedding model `nomic-embed-text:latest` is installed and returns exactly 768 dimensions.
- Three chunks were embedded successfully.
- Thai semantic paraphrase query returned the canonical Knowledge only with similarity about 0.52–0.57.
- Old duplicate test Knowledge records were soft-forgotten, not hard deleted.
- Original source file remains local; no binary upload was introduced.
- Active Project security boundary remains enforced.
- Re-ingestion now reconciles chunk sets only after the new set saves completely; stale old chunks are archived instead of remaining active.
- Remote Runtime allowlist exposes V2 read-only `knowledge.semantic_search`, `knowledge.graph`, and `knowledge.sources`, but still blocks `knowledge.ingest_file`, raw process and shell execution.

## Runtime code added/changed

Desktop repository changes include:

- `apps/desktop/ceo-knowledge-ingest.mjs`
- `apps/desktop/advanced-tools.mjs` — `ollama.embed`
- `apps/desktop/ceo-knowledge-tools.mjs` — ingestion/semantic/sources/graph tools
- `apps/desktop/ceo-knowledge-service.mjs` — stable file-backed dedup
- `apps/desktop/ceo-runtime.mjs` — V2 tool wiring
- targeted V2 ingestion/dedup tests

Runtime backups created before V2 activation are under:

- `D:\Ceo-MCP-Agent-ADMIN\.ceo\knowledge-v2-backup-*`

## Verification already passed

- targeted Knowledge/Ingestion tests: 12/12
- TypeScript typecheck passed during implementation
- Runtime tool registry exposed V2 capabilities
- Runtime ingestion + semantic retrieval E2E passed
- Chunk reconciliation E2E passed: changing chunk size replaced 3 active chunks with 7 new active chunks and archived the 3 stale chunks
- Full Desktop test suite passed: 223/223, fail 0
- Desktop TypeScript typecheck passed after final V2 changes
- Cloud repo `npm run verify` passed after final shared remote-tool contract changes

## Post-commit deployment note

- Cloud source commit `de2082b` passed `npm run verify` and is pushed to `origin/main`.
- Production Worker remains healthy at `https://ceo.disomanceo.workers.dev`.
- Redeploy of the new V2 read-only remote allowlist was attempted from SCHOOL-PC but Wrangler refused non-interactive deployment because this machine has no Cloudflare API token/OAuth session. No production change occurred.
- Run `npm run deploy:worker` from a Cloudflare-authenticated machine. Do not use `wrangler --temporary` for production.
- Until that redeploy, the current production Worker continues serving the previous safe allowlist; local V2 ingestion/semantic search is unaffected.

## Hybrid Retrieval — E2E PROVEN

- Desktop full test suite after Hybrid Retrieval: 226/226, fail 0.
- Targeted Hybrid/Knowledge tests: 15/15.
- `knowledge.recall` runs keyword first, semantic second, then merges/deduplicates by Knowledge entry.
- Thai paraphrase E2E: keyword-only returned 0 results; Hybrid returned canonical Knowledge id `24685cf0-dcbb-4559-b1d4-e7b97e875973`.
- Semantic model: `nomic-embed-text:latest`.
- Project-scoped recall intentionally stays keyword-only until semantic project filtering is implemented.
- Runtime activation used Blue/Green: new Runtime 8796, previous 8795 drained.
- Runtime backup before Hybrid activation: `D:\Ceo-MCP-Agent-ADMIN\.ceo\hybrid-retrieval-backup-20260828-103913`.

## Graph Auto-link — E2E PROVEN

- Desktop full suite after Graph hardening: 229/229, fail 0.
- Targeted Graph/Knowledge/Hybrid tests: 18/18.
- Temp Launcher test harness startup budget increased from 15s to 30s only for full-suite concurrency; targeted Launcher tests pass 5/5. Production Launcher logic was not changed for this.
- SCHOOL-PC Runtime activated through Blue/Green on port 8797.
- Runtime backup: `D:\Ceo-MCP-Agent-ADMIN\.ceo\graph-autolink-backup-20260828-105115`.
- Real E2E source: `D:\Ceo-MCP-Agent-ADMIN\docs\ARCHITECTURE.md`.
- Created Knowledge `8c52d666-d3c2-4e34-892c-7ac486754734`, 2 chunks / embedding 2/2.
- Default auto-link threshold 0.60 found canonical Ceo Knowledge at similarity ~0.7671 and created one `related_to` relation.
- Re-ingest kept the same source/Knowledge/link ids; graph remained 2 active nodes / 1 link.
- Graph API filters links whose endpoint Knowledge is no longer active.

## Next implementation order

1. Graph UI / mobile visualization.
2. Obsidian connector.
3. Google Drive connector.
4. Google Calendar sync with explicit conflict policy.
5. Web Push / follow-up scheduler.
6. Backup/export/restore tooling.
7. Versioned contract tests between `ceo-knowledge` and `Ceo-MCP-Agent`.

## Safety rules remain unchanged

- Ceo MCP Agent startup must not depend on Supabase, Cloudflare, vector search, Ollama or connectors.
- Never use `supabase db reset` against Maple.
- Never mutate existing Maple application schemas to fix Ceo Knowledge; forward-fix only inside `ceo_knowledge` unless explicitly approved.
- Never expose raw filesystem/PowerShell remotely; keep device/job allowlists and Active Project boundaries.
- Original local files stay local by default.
