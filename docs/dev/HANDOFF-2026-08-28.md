# Handoff — 2026-08-28 — SCHOOL-PC

Use this as the current continuation checkpoint. The 2026-08-27 handoff remains as historical context.

## Repositories

- Cloud/Knowledge: `D:\AI-Workspace\ceo-knowledge` — https://github.com/disomanceo/ceo-knowledge
- Desktop Runtime: `D:\AI-Workspace\Ceo-MCP-Agent` — https://github.com/disomanceo/ceo-MCP-Agent
- SCHOOL-PC runtime install: `D:\Ceo-MCP-Agent-ADMIN`

## Production

- Worker: `https://ceo.disomanceo.workers.dev`
- Mobile PWA: `https://ceo-knowledge.pages.dev`
- Supabase Maple: project ref `pcvdtcntyzndhfxfawbo`
- isolated schema: `ceo_knowledge`

## SCHOOL-PC / V1.2

- Device Agent starts and reaches Cloud successfully.
- Runtime/Launcher recovery was exercised while installing V2 and returned to RUNNING.
- SCHOOL-PC six-digit pairing was completed through the production authenticated Pair API using the same locally DPAPI-protected Ceo member session. Device `d76ede57-0030-4ebb-be28-9753fb0e28d6` is now TRUSTED + ONLINE. Manual tap-through of the Mobile pairing form remains a UX-only check, not a backend blocker.

## V2 File Ingestion — E2E PROVEN

Completed on SCHOOL-PC:

```text
Active Project local file
 -> document.verify/read
 -> source metadata + sha256
 -> Ollama extraction
 -> Knowledge entry
 -> bounded chunks
 -> 768-d local embedding
 -> Supabase ceo_knowledge
 -> semantic retrieval
```

Proof points:

- `knowledge.ingest_file` completed against `D:\Ceo-MCP-Agent-ADMIN\docs\CEO-KNOWLEDGE.md`.
- Source dedup returned the same source id for repeated ingestion.
- File-backed Knowledge dedup bug was fixed: source + content fingerprint no longer depends on AI/caller title.
- Repeated ingestion with two different titles returned the same canonical Knowledge id `24685cf0-dcbb-4559-b1d4-e7b97e875973`.
- Local embedding model `nomic-embed-text:latest` is installed and returns exactly 768 dimensions.
- Three chunks were embedded successfully.
- Thai semantic paraphrase query returned the canonical Knowledge only with similarity about 0.52–0.57.
- Old duplicate test Knowledge records were soft-forgotten, not hard deleted.
- Original source file remains local; no binary upload was introduced.
- Active Project security boundary remains enforced.
- Re-ingestion now reconciles chunk sets only after the new set saves completely; stale old chunks are archived instead of remaining active.
- Remote Runtime allowlist exposes V2 read-only `knowledge.semantic_search`, `knowledge.graph`, and `knowledge.sources`, but still blocks `knowledge.ingest_file`, raw process and shell execution.

## Runtime code added/changed

Desktop repository changes include:

- `apps/desktop/ceo-knowledge-ingest.mjs`
- `apps/desktop/advanced-tools.mjs` — `ollama.embed`
- `apps/desktop/ceo-knowledge-tools.mjs` — ingestion/semantic/sources/graph tools
- `apps/desktop/ceo-knowledge-service.mjs` — stable file-backed dedup
- `apps/desktop/ceo-runtime.mjs` — V2 tool wiring
- targeted V2 ingestion/dedup tests

Runtime backups created before V2 activation are under:

- `D:\Ceo-MCP-Agent-ADMIN\.ceo\knowledge-v2-backup-*`

## Verification already passed

- targeted Knowledge/Ingestion tests: 12/12
- TypeScript typecheck passed during implementation
- Runtime tool registry exposed V2 capabilities
- Runtime ingestion + semantic retrieval E2E passed
- Chunk reconciliation E2E passed: changing chunk size replaced 3 active chunks with 7 new active chunks and archived the 3 stale chunks
- Full Desktop test suite passed: 223/223, fail 0
- Desktop TypeScript typecheck passed after final V2 changes
- Cloud repo `npm run verify` passed after final shared remote-tool contract changes

## Cloudflare deployment state — RESOLVED

- Wrangler OAuth login was completed successfully on SCHOOL-PC on 2026-08-28.
- Production Worker is deployed and healthy at `https://ceo.disomanceo.workers.dev`.
- Current production Worker version after Remote Console + Ollama fallback hardening: `0ca726f0-2281-4a2b-aab7-16cdfe87d0e0`.
- Production Pages is deployed at `https://ceo-knowledge.pages.dev`.
- Current production Mobile assets after Console-first deployment: `/assets/index-BRXz7uJU.js` + `/assets/index-OHnPaigK.css`.

## Hybrid Retrieval — E2E PROVEN

- Desktop full test suite after Hybrid Retrieval: 226/226, fail 0.
- Targeted Hybrid/Knowledge tests: 15/15.
- `knowledge.recall` runs keyword first, semantic second, then merges/deduplicates by Knowledge entry.
- Thai paraphrase E2E: keyword-only returned 0 results; Hybrid returned canonical Knowledge id `24685cf0-dcbb-4559-b1d4-e7b97e875973`.
- Semantic model: `nomic-embed-text:latest`.
- Project-scoped recall intentionally stays keyword-only until semantic project filtering is implemented.
- Runtime activation used Blue/Green: new Runtime 8796, previous 8795 drained.
- Runtime backup before Hybrid activation: `D:\Ceo-MCP-Agent-ADMIN\.ceo\hybrid-retrieval-backup-20260828-103913`.

## Graph Auto-link — E2E PROVEN

- Desktop full suite after Graph hardening: 229/229, fail 0.
- Targeted Graph/Knowledge/Hybrid tests: 18/18.
- Temp Launcher test harness startup budget increased from 15s to 30s only for full-suite concurrency; targeted Launcher tests pass 5/5. Production Launcher logic was not changed for this.
- SCHOOL-PC Runtime activated through Blue/Green on port 8797.
- Runtime backup: `D:\Ceo-MCP-Agent-ADMIN\.ceo\graph-autolink-backup-20260828-105115`.
- Real E2E source: `D:\Ceo-MCP-Agent-ADMIN\docs\ARCHITECTURE.md`.
- Created Knowledge `8c52d666-d3c2-4e34-892c-7ac486754734`, 2 chunks / embedding 2/2.
- Default auto-link threshold 0.60 found canonical Ceo Knowledge at similarity ~0.7671 and created one `related_to` relation.
- Re-ingest kept the same source/Knowledge/link ids; graph remained 2 active nodes / 1 link.
- Graph API filters links whose endpoint Knowledge is no longer active.

## Mobile Graph UI — SOURCE VERIFIED

- Added a sixth Mobile tab: `Graph`, without changing Chat/Today/Memory/Tasks/Devices behavior.
- Mobile reads `ceo_knowledge.knowledge_entries` and `knowledge_links` directly through the logged-in Supabase session and RLS; it does not require Worker Graph API or a running Runtime.
- No `service_role` or device secret is added to Mobile. Only the existing public Supabase publishable key + user access token are used.
- Shared graph contract now owns active-node/link filtering and deterministic radial layout so Mobile and future Worker output use the same rules.
- UI uses native SVG, graph/list toggle, search, node detail, relation navigation and relationship weights; no heavy graph dependency was added.
- Worker `/api/graph` hardening is deployed; Cloudflare authentication is resolved on SCHOOL-PC.
- Cloud repo `npm run verify` passed after Graph UI: Shared/Mobile/Worker typecheck, Worker tests 4/4, Shared tests 4/4, Mobile production build, Worker dry-run.
- Graph UI is production-deployed and bundle-verified.

## Graph UI deployment note — DEPLOYED

- Graph UI source is included in the current production Pages deployment.
- Production bundle verification confirmed the deployed JavaScript contains `Knowledge Graph` and the current CSS/JS asset fingerprint matches the latest build.

## Ceo Local Notes — E2E PROVEN

- Native Ceo module: `apps/desktop/ceo-local-notes.mjs`.
- No third-party notes SDK/API/runtime/package dependency; runtime module imports Node core only.
- Supports open Markdown `.md` / `.markdown` under the existing Active Project/workspace scanner boundary.
- `knowledge.local_notes_scan` is read-only; `knowledge.local_notes_import` defaults to dry-run and remains local-only (not Remote Safe).
- Desktop targeted Knowledge/Local Notes tests: 19/19.
- Desktop TypeScript typecheck passed.
- Desktop full suite: 233/233, fail 0.
- SCHOOL-PC Blue/Green activated Runtime 8798; previous 8797 drained.
- Backup: `D:\Ceo-MCP-Agent-ADMIN\.ceo\local-notes-backup-20260828-112627`.
- E2E scan of `docs` found Markdown correctly and dry-run returned an import plan without writing Knowledge.
- Actual import of `docs\ADAPTER-CONTRACT.md` created/returned Knowledge `53850b63-c00c-4d83-9251-4382c4de4e86` and Source `66f3ca29-b71a-46e9-baec-7f26e559002b`.
- Graph auto-link created two semantic relations (~0.755 and ~0.703). Re-import kept the same Knowledge/Source/link ids; graph remained 3 active nodes / 2 links.
- No Supabase migration or existing Maple object change was required for Ceo Local Notes.
- Historical migration text may still contain a previously reserved provider label; it is migration history only and is not used by Ceo Local Notes. Do not rewrite deployed migration history.

## Ceo Drive V1 + Mobile Chat Layout — DEPLOYED / TEST READY

- Cloudflare Wrangler OAuth was completed successfully on SCHOOL-PC on 2026-08-28.
- Production deployment completed after commit `b6126ab`: Worker + Pages both succeeded from SCHOOL-PC after Wrangler OAuth login.
- Production verification: Pages HTTP 200, Worker health `ok=true`, unauthenticated Drive API returns 401 `AUTH_REQUIRED`, and production bundle contains `.chat-shell`, `.chat-composer`, `Ceo Drive`, `Setup Required`, `drive.readonly`, and `Knowledge Graph`.
- Current production Mobile assets: `index-DtUoTPwZ.js` and `index-Ch2NbdRh.css`.
- Chat composer was moved upward using `100dvh` layout and additional composer/bottom-nav spacing so it no longer sits against/under the fixed navigation bar.
- Mobile now has 7 tabs: Chat / Today / Memory / Tasks / Graph / Drive / Devices.
- Ceo Drive V1 uses Google Drive read-only as the first backend but keeps the Ceo-owned API/UI contract.
- OAuth design uses Supabase identity linking with `drive.readonly`; Google provider token is captured only in browser `sessionStorage`, sent per-request in `x-ceo-drive-token`, and never persisted by the Worker.
- No new Supabase migration and no `service_role` were introduced.
- Supported Cloud imports: Google Docs→Markdown, Sheets→CSV, Slides→text, Apps Script→JSON, and normal text/Markdown/CSV/JSON/XML/YAML.
- PDF/Office formats explicitly return `RUNTIME_IMPORT_REQUIRED` in V1.
- Worker import path writes source metadata + ingest run + Knowledge + chunks using the signed-in user's existing RLS.
- Cloud repo verification after Ceo Drive: Worker tests 12/12, Shared tests 4/4, all Shared/Mobile/Worker typechecks pass, Mobile production build and Worker dry-run pass.
- Real Maple check: Supabase Auth settings currently report `external.google=false`; real Google Drive OAuth cannot be E2E-tested until a Google OAuth client is enabled in Supabase.
- No existing Google OAuth client credentials were found in the known Ceo/Personal MCP workspaces, and `gcloud` is not installed on SCHOOL-PC. Do not fabricate or commit OAuth credentials.
- Detailed setup/test checklist: `docs/dev/CEO-DRIVE.md`.

## Cloud Chat fallback — SUPERSEDED BY AUTO RUNTIME ROUTER

- The temporary Knowledge-only fallback fixed the original misleading `ไม่พบข้อมูล` UX.
- It is no longer the primary fallback path when a trusted Runtime is online: AUTO Router now sends general fallback chat to local Ollama first, then uses Cloud/Knowledge fallback if Runtime AI is unavailable.
- No paid Cloud LLM secret is required for the current trial path.

## AUTO Chat Router → Local Ollama — PRODUCTION E2E PROVEN / FALLBACK ROLE

- Cloud Chat provider order is now deterministic secretary intents first, then trusted online Runtime Ollama, then optional Cloud LLM, then Ceo Knowledge fallback.
- Worker selects only a TRUSTED device seen within 60 seconds whose reported remote tools include `ollama.chat`.
- Chat creates a normal `runtime_jobs` row with origin `worker`, tool `ollama.chat`, no approval-required mutation, 10-minute expiry and a unique idempotency key.
- Initial production E2E used `qwen3:4b` successfully. After latency comparison, default fallback was changed to `qwen2.5vl:3b`; Worker still supports `OLLAMA_CHAT_MODEL` override. Final production E2E returned the correct READY/model answer in ~5.3s (34 output tokens, ~88.8 tok/s).
- Prompt includes bounded Ceo Knowledge context when available and prepends `/no_think` for Qwen3. System prompt defines `Ceo` as the product's AI assistant name, not the corporate CEO role.
- Direct SCHOOL-PC Ollama test after hardening returned exactly `Ceo Ollama พร้อมทดสอบแล้ว`.
- Mobile polls the existing Runtime Job API until completed/failed/expired/timeout and displays provider badges such as `AUTO · OLLAMA qwen3:4b`, `AUTO · KNOWLEDGE`, or `CLOUD · SECRETARY`.
- If Ollama returns unavailable/fails, Mobile uses the Worker-provided Knowledge fallback instead of leaving the chat stuck.
- Worker unit tests now include Runtime/Ollama device selection, stale/untrusted rejection, prompt/context construction and job-result extraction.
- Production full-route E2E passed: `/api/chat` → runtime job → trusted SCHOOL-PC Device Agent → `ollama.chat` → completed result. Runtime job insert permission bug was fixed without granting authenticated UPDATE.
- Latest local verify after Console + fallback optimization: Worker tests 23/23, Shared tests 4/4, all typechecks pass, Mobile build and Worker dry-run pass.
- Production Recent Jobs API E2E: authenticated `GET /api/runtime/jobs?limit=5` returned HTTP 200 and recent completed Ollama jobs.
- Production Pages bundle contains `Ceo Remote Console`, `Secretary Dashboard`, fallback AI copy, and Console CSS.
- SCHOOL-PC Device Agent Pair API E2E completed: TRUSTED=true, ONLINE=true, pairing cleared.

## Product Role Lock — MOBILE REMOTE CONSOLE / NO CORE REWRITE

- Decision: do not rebuild Ceo Mobile as a ChatGPT clone and do not re-architect the existing Core.
- ChatGPT is the primary conversational brain/UI; Ceo remains the durable Knowledge/Secretary/Tools/Runtime system.
- Ceo Mobile is now explicitly **Remote Console + Secretary Dashboard**. Default tab is Console, not Chat.
- Console aggregates Today, open Tasks, trusted/online Devices, recent Remote Jobs and Drive readiness, then links into the existing feature pages.
- Bottom navigation remains seven items: Console / Chat / Today / Tasks / Graph / Drive / Devices. Memory remains unchanged and is reached from Console.
- Chat/Ollama is labelled fallback AI. Existing AUTO Router stays; default fallback model changed to `qwen2.5vl:3b` because local text-only comparison returned 29 output tokens at ~83 tok/s and ~8.5s elapsed, substantially lighter than Qwen3 reasoning overhead in the current setup. Qwen3 control token is now model-conditional only.
- Added authenticated GET `/api/runtime/jobs?limit=N` for bounded recent-job visibility in Console. It uses existing owner RLS/select permission and adds no database grant/migration.
- No Desktop/Core source change is required for this product-role shift.
- Canonical role document: `docs/dev/PRODUCT-ROLE.md`.
- Production commit: `51370b3 — feat: make Ceo Mobile a remote console`. Readiness follow-up: `7208531 — fix: report Ollama fallback readiness accurately`.

## Next implementation order

1. Deploy/verify the Console-first Mobile build and manually validate it on a phone.
2. Add device revoke/disable and bounded approval-review UX.
3. Enable Google OAuth provider in Maple and run real Ceo Drive E2E.
4. Ceo Drive Runtime handoff for PDF/Office + Calendar connector.
5. Web Push/follow-up scheduler.
6. Backup/export/restore + versioned contract/release hardening.

## Safety rules remain unchanged

- Ceo MCP Agent startup must not depend on Supabase, Cloudflare, vector search, Ollama or connectors.
- Never use `supabase db reset` against Maple.
- Never mutate existing Maple application schemas to fix Ceo Knowledge; forward-fix only inside `ceo_knowledge` unless explicitly approved.
- Never expose raw filesystem/PowerShell remotely; keep device/job allowlists and Active Project boundaries.
- Original local files stay local by default.
